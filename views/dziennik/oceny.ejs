<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Szkoła Podstawowa nr. 1 w Morzeszczynie</title>

    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css" />
    <script src="/js/bootstrap.js"></script>
</head>
<body class="d-flex flex-column min-vh-100">
    <%- include("../navbar.ejs"); %>


    <header class="container text-center d-flex flex-column align-items-center mt-5 mb-5">
        <h3 class="display-3 mt-5">Twoje Oceny</h3>
    </header>


    <h4 class="w-100 text-center">
        <p>
            Średnia ogólna z wszystkich przedmiotów:
            <%= sredniaTotal%>
        </p>
    </h4>

    <br>
    
    <main class="d-flex w-100 flex-column align-items-center justify-content-center">
        <table class="table w-50">
            <thead>
              <tr>
                <th scope="col">Przedmiot</th>
                <th scope="col">Średnia ocen</th>
              </tr>
            </thead>
            <tbody>

                <% for(var i=0; i <= totalOcenySrednia - 1; i++) {%>
                    <tr>
                        <th><%= ocenySrednia[i].nazwa%></th>
                        <td><%= ocenySrednia[i].srednia%></td>
                    </tr>
                <% } %>
            </tbody>
          </table>


          <hr>
          <h4 class="w-100 text-center">Oceny cząstkowe</h4>


        <table class="table w-50">
            <thead>
              <tr>
                <th scope="col">Ocena</th>
                <th scope="col">Przedmiot</th>
                <th scope="col">Waga</th>
                <th scope="col">Data</th>
              </tr>
            </thead>
            <tbody>

                <% for(var i=0; i <= totalOceny - 1; i++) {%>
                    <tr>
                        <th><%= oceny[i].ocena%></th>
                        <td><%= oceny[i].nazwa%></td>
                        <td><%= oceny[i].waga%></td>
                        <td><%= oceny[i].data%></td>
                    </tr>
                <% } %>
            </tbody>
          </table>

    </main>

 
    <%- include("../footer.ejs"); %>

</body>
</html>
